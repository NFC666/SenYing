<UserControl x:Class="SenYing.WPF.Views.UserControls.IndexUserControl"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:SenYing.WPF.Views.UserControls"
             mc:Ignorable="d" 
             xmlns:behaviors="clr-namespace:SenYing.WPF.Behaviors"
             xmlns:i="http://schemas.microsoft.com/xaml/behaviors"
             xmlns:vwp="clr-namespace:WpfToolkit.Controls;assembly=VirtualizingWrapPanel"
             xmlns:converter="clr-namespace:SenYing.WPF.Converter"
             xmlns:vm="clr-namespace:SenYing.WPF.ViewModels" 
             d:DataContext="{d:DesignInstance vm:IndexUserControlVm,IsDesignTimeCreatable=True}"
             d:DesignHeight="450" d:DesignWidth="800">
  <UserControl.Resources>
    <converter:UriToImageConverter x:Key="UriToImageConverter"/>
    <converter:BoolToVisibilityConverter x:Key="BoolToVisibilityConverter"/>
  </UserControl.Resources>
  <Grid>
    <ProgressBar
      Style="{StaticResource MaterialDesignCircularProgressBar}"
      IsIndeterminate="{Binding IsBusy}"
      Visibility="{Binding IsBusy,Converter={StaticResource BoolToVisibilityConverter}}"
      VerticalAlignment="Center"/>
    <ListView

      VirtualizingPanel.IsVirtualizing="True"
      VirtualizingPanel.VirtualizationMode="Recycling"
      SelectedItem="{Binding SelectedVideoInfo}"
      ItemsSource="{Binding VideoInfos}">
      <i:Interaction.Behaviors>
        <behaviors:ScrollToEndBehavior Command="{Binding LoadMoreCommand}" />
      </i:Interaction.Behaviors>
      <ListView.ItemsPanel>
        <ItemsPanelTemplate>
          <vwp:VirtualizingWrapPanel />
        </ItemsPanelTemplate>
      </ListView.ItemsPanel>

      <ListView.ItemTemplate>
        <DataTemplate>
          <StackPanel
            Width="200"
            Height="350"
            HorizontalAlignment="Center"
            VerticalAlignment="Center">
            <Image
                Width="200"
                Source="{Binding CoverUrl, Converter={StaticResource UriToImageConverter}}"
                HorizontalAlignment="Center"/>
            <TextBlock
                Margin="5"
                TextAlignment="Center"
                FontSize="15"
                FontWeight="Bold"
                TextWrapping="Wrap"
                Text="{Binding Name}"/>
          </StackPanel>
        </DataTemplate>
      </ListView.ItemTemplate>
    </ListView>
  </Grid>
</UserControl>
