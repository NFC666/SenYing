<UserControl x:Class="SenYing.WPF.Views.UserControls.SearchViewUserControl"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:SenYing.WPF.Views.UserControls" xmlns:converter="clr-namespace:SenYing.WPF.Converter" xmlns:vwp="clr-namespace:WpfToolkit.Controls;assembly=VirtualizingWrapPanel" xmlns:i="http://schemas.microsoft.com/xaml/behaviors" xmlns:behaviors="clr-namespace:SenYing.WPF.Behaviors"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800">
  <UserControl.Resources>
    <converter:UriToImageConverter x:Key="UriToImageConverter"/>
    <converter:BoolToVisibilityConverter x:Key="BoolToVisibilityConverter"/>
  </UserControl.Resources>
  <Grid>
    <ProgressBar
      Style="{StaticResource MaterialDesignCircularProgressBar}"
      IsIndeterminate="{Binding IsBusy}"
      Visibility="{Binding IsBusy,Converter={StaticResource BoolToVisibilityConverter}}"
      VerticalAlignment="Center"/>
    <ListView

      VirtualizingPanel.IsVirtualizing="True"
      VirtualizingPanel.VirtualizationMode="Recycling"
      SelectedItem="{Binding SelectedVideoInfo}"
      ItemsSource="{Binding VideoInfos}">
      <i:Interaction.Behaviors>
        <behaviors:ScrollToEndBehavior Command="{Binding LoadMoreCommand}" />
      </i:Interaction.Behaviors>
      <ListView.ItemsPanel>
        <ItemsPanelTemplate>
          <vwp:VirtualizingWrapPanel HorizontalAlignment="Stretch"/>
        </ItemsPanelTemplate>
      </ListView.ItemsPanel>

      <ListView.ItemTemplate>
        <DataTemplate>
          <Grid>
            <StackPanel Width="200">
              <Image
                Width="180"
                Source="{Binding CoverUrl, Converter={StaticResource UriToImageConverter}}"/>
              <TextBlock
                Margin="5"
                TextAlignment="Center"
                FontSize="15"
                FontWeight="Bold"
                TextWrapping="Wrap"
            
                Text="{Binding Name}"/>
            </StackPanel>
          </Grid>
        </DataTemplate>
      </ListView.ItemTemplate>
    </ListView>
  </Grid>
</UserControl>
